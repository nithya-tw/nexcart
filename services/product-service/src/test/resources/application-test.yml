spring:
  cache:
    type: none  # Disable caching for tests
  data:
    redis:
      host: disabled
      port: 0
  flyway:
    enabled: true
    baseline-on-migrate: true
  jpa:
    show-sql: false  # Reduce noise in test logs
    hibernate:
      ddl-auto: validate
